import 'package:flutter/material.dart';
import 'package:plugins_java/src/app.dart';

void main() {
  runApp(MaterialApp(
      title: 'Twitter Channel',
      theme: ThemeData(primarySwatch: Colors.green),
      home: App()));
}
